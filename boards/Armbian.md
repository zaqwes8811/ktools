# Build for `aio3568j`

```
# Can't run multiline command

./compile.sh BOARD=bananapim7 BRANCH=edge KERNEL_BTF=no BUILD_MINIMAL=yes BUILD_DESKTOP=no RELEASE=noble KERNEL_GIT=shallow KERNEL_CONFIGURE=no PRIVATE_CCACHE=yes
```